// @flow
import React from 'react';
import {View} from 'react-native';
import type {UserType} from '@types/base';

type PropsType = {
    user: UserType
}

const Component = ({user}: PropsType) => {
    return (
        <View>

        </View>
    );
};

export default Component;
